# Docker-specific environment configuration
# This file is used by docker-compose.yml

# Google Drive Configuration
# IMPORTANT: This path must match the volume mount in docker-compose.yml
GOOGLE_SERVICE_ACCOUNT_FILE=/app/somer-services-458421-ee757e0c4238.json
GOOGLE_DRIVE_FOLDER_ID=1eF-ZrwU8GdSTa8uRSOlbvdxzoG64vsYk

# S3-Compatible Object Storage
S3_ENDPOINT=https://s3.amazonaws.com
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
VECTOR_STORE_ID=vs_...

# Processing Configuration
# Maximum files to process in one run (for testing)
MAX_FILES_PER_RUN=10
# Supported file extensions (comma-separated)
# NOTE: This is for non-Google Workspace files only
# Google Docs/Sheets/Slides are automatically exported regardless of this setting
SUPPORTED_EXTENSIONS=.pdf,.doc,.docx,.ppt,.pptx,.txt,.rtf,.epub

# Database Configuration
# CRITICAL: Database must be in the persistent volume mounted at /app/data
# Without this, the database will be lost when the container restarts!
DATABASE_PATH=/app/data/pipeline.db

